from setuptools import setup, find_packages

setup(
    name='pybot01',
    version='0.1',
    packages=find_packages(),
    install_requires=['yfinance'],
    description='A beginner Python bot for ticker info',
    author='Your Name',
)
